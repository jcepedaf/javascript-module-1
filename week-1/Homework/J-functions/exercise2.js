function triple(number) {
  // complete function here
  var resultado = number*3; //multiplico por tres la entrada de la funcion y el valor se lo agrego a la varible resultado
  return resultado;//retorno el resultado
}

var result = triple(12); //declaro la variable result y le agrego el valor de la funcion triple con entrada de 12

console.log(result); //imprimo result
