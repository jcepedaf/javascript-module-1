// Complete the function so that it takes input parameters
function multiply(a,b) {//agrego parametros de entrada a la funcion llamados a y b
  // Calculate the result of the function and return it
  return a*b; //resalizo multiplicacion de a*b y devuelvo el resultado
}

// Assign the result of calling the function the variable `result`
var result = multiply(3, 4); //llamo la funcion multiply con valor de parametro 3 y 4 y el resultado se lo asigno a la variable result

console.log(result);//imprimo el valor de result en pantalla
